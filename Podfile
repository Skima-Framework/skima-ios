ENV['SWIFT_VERSION'] = '5.0'
platform :ios, '11.0'
install! 'cocoapods', :disable_input_output_paths => true
use_modular_headers!
use_frameworks!

target 'SkimaTestApp' do
  pod 'Skima', :path => './'

end
